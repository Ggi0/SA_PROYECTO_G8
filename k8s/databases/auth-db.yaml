apiVersion: v1
kind: PersistentVolumeClaim
metadata: { name: auth-db-pvc, namespace: quetxal-tv-prod }
spec:
  accessModes: [ReadWriteOnce]
  resources: { requests: { storage: 3Gi } }
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: auth-db, namespace: quetxal-tv-prod }
spec:
  replicas: 1
  selector: { matchLabels: { app: auth-db } }
  strategy:
    type: Recreate
  template:
    metadata: { labels: { app: auth-db } }
    spec:
      containers:
        - name: auth-db
          image: ${ARTIFACT_REGISTRY_LOCATION}-docker.pkg.dev/${GCP_PROJECT_ID}/${ARTIFACT_REGISTRY_REPOSITORY}/auth-db:${IMAGE_TAG}
          ports: [{ containerPort: 5432 }]
          resources:
            requests: { cpu: "50m", memory: "128Mi" }
            limits: { cpu: "250m", memory: "384Mi" }
          env:
            - name: POSTGRES_USER
              valueFrom: { secretKeyRef: { name: quetxal-tv-secrets, key: AUTH_DB_USER } }
            - name: POSTGRES_PASSWORD
              valueFrom: { secretKeyRef: { name: quetxal-tv-secrets, key: AUTH_DB_PASSWORD } }
            - name: POSTGRES_DB
              valueFrom: { secretKeyRef: { name: quetxal-tv-secrets, key: AUTH_DB_NAME } }
          volumeMounts: [{ name: data, mountPath: /var/lib/postgresql/data }]
          readinessProbe: { exec: { command: ["sh", "-c", "pg_isready -U \"$POSTGRES_USER\" -d \"$POSTGRES_DB\""] }, initialDelaySeconds: 10, periodSeconds: 10, failureThreshold: 5 }
          livenessProbe: { exec: { command: ["sh", "-c", "pg_isready -U \"$POSTGRES_USER\" -d \"$POSTGRES_DB\""] }, initialDelaySeconds: 20, periodSeconds: 30, failureThreshold: 3 }
      volumes: [{ name: data, persistentVolumeClaim: { claimName: auth-db-pvc } }]
---
apiVersion: v1
kind: Service
metadata: { name: auth-db, namespace: quetxal-tv-prod }
spec:
  type: ClusterIP
  selector: { app: auth-db }
  ports: [{ port: 5432, targetPort: 5432 }]
