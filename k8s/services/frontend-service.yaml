apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: quetxal-tv-prod
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
