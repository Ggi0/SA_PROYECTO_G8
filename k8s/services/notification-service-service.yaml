apiVersion: v1
kind: Service
metadata: { name: notification-service, namespace: quetxal-tv-prod }
spec:
  type: ClusterIP
  selector: { app: notification-service }
  ports: [{ port: 50056, targetPort: 50056 }]
