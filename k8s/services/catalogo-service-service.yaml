apiVersion: v1
kind: Service
metadata: { name: catalogo-service, namespace: quetxal-tv-prod }
spec:
  type: ClusterIP
  selector: { app: catalogo-service }
  ports: [{ port: 50052, targetPort: 50052 }]
